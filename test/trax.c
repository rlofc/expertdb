#include "c89spec/c89spec.h"
#include <stdio.h>

describe (trax_module) {

   it (should initialize a graph manipulation transaction) {
      /* trage_begin(gstor_ptr); */
   }

   it (should conclude a graph manipulation transaction) {
      /* trage_eng(gstor_ptr); */
   }

   it (should keep manipulation context atomic) {
      /* trage_t * t = trage_begin(gstor_ptr); */

      /* trage_add_node(t); */
      /* trage_del_node(t); */
      /* trage_link(t); */

      /* trage_end(t); */
   }

}
